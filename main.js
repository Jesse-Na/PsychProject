class Word {
	constructor(word) {
		this.word = word;
		this.x = random(width);
	}

}

function setup() {
	createCanvas(displayWidth, displayHeight);

}
function draw() {
	ellipse(200, 200, 200, 200);
}
